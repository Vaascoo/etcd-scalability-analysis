%{ for host in host_list ~}
${host.name} ansible_host=${host.network_interface.0.access_config.0.nat_ip} ansible_user=ubuntu ansible_python_interpreter=/usr/bin/python3 ansible_connection=ssh
%{ endfor ~}

[targets]
%{ for host in host_list ~}
${host.name}
%{ endfor ~}
